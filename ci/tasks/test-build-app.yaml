platform: linux
inputs:
  - name: app-build
outputs:
  - name: app-test-build
run:
  path: sh
  args:
    - -c
    - |
      set -ex
      echo "test app"
      cwd="$(pwd)"
      cd app-prepared
        npm test
        npm run test-dapp
      cd "${cwd}"
      cp -fa app-build/Dockerfile app-test-build/
      cp -fa app-build/dist/ app-test-build/
      mkdir -p repo/.git/
      cp -fa repo/.git/head_sha app-test-build/.git/ref
